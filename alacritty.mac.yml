env:
  TERM: xterm-256color

window:
  padding:
    x: 0
    y: 0

font:
  normal:
    family: "MesloLGS NF"
  size: 12

shell:
  program: /bin/zsh
  args:
    - -l
    - -c
    - "source ~/.zshrc; tmux attach 2> /dev/null || tmux"

alt_send_esc: false
